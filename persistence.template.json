{
  "UiMode": "Tui",
  "ProposalApproval": "Self",
  "MaxActionIterations": 5,
  "DebugMode": false,

  "_SeedsDirectory_comment": "Optional. Folder of per-database identity seed files; {dbName}.json seeds a brand-new {dbName}.db with authored identity fragments. Blank (default) = a 'seeds' folder beside the database directory.",
  "SeedsDirectory": "",

  "_SharedDirectory_comment": "Optional host folder bridged into the peer's container as /shared (see container/docker-compose.yml — its mount must point at this same host path). The snapshot_db command writes a read-only DB copy here. Blank disables snapshot_db.",
  "SharedDirectory": "",

  "SelectedLocalPeer": "Local Peer",
  "LocalPeers": [
    { "Name": "Local Peer", "Description": "the person at the keyboard" }
  ],

  "SelectedModel": "anthropic",
  "Models": [
    {
      "Name": "anthropic",
      "Provider": "Anthropic",
      "Model": "claude-opus-4-8",
      "DatabasePath": "anthropic.db",
      "ApiKey": "YOUR_API_KEY_HERE",
      "ApiBaseUrl": null,
      "_ContainerName_comment": "Optional. Binds this peer to its own sandboxed 'computer' container (add a service in container/docker-compose.yml, e.g. persistence-claude-computer), overriding the shared Container.Name while this profile is active. null = share the default box.",
      "ContainerName": null,
      "_ContainerAllowAll_comment": "Optional per-participant override. true = let this peer run any program (allowlist bypassed; the container's isolation is still the boundary). false = force the allowlist on. null = inherit the shared Container.AllowAllCommands.",
      "ContainerAllowAll": null,
      "MaxInputTokens": 200000,
      "MaxOutputTokens": 32000,
      "ReasoningEffort": "off",
      "Streaming": true,
      "RequestTimeoutSeconds": 600
    },
    {
      "Name": "cloud",
      "Provider": "OpenAI",
      "Model": "gpt-5.5",
      "DatabasePath": "cloud.db",
      "ApiKey": "YOUR_API_KEY_HERE",
      "ApiBaseUrl": null,
      "MaxInputTokens": 8000,
      "MaxOutputTokens": 32000,
      "ReasoningEffort": "off",
      "Streaming": true,
      "RequestTimeoutSeconds": 600
    },
    {
      "Name": "local",
      "Provider": "OpenAiChat",
      "Model": "local",
      "DatabasePath": "local.db",
      "ApiKey": "",
      "ApiBaseUrl": "http://127.0.0.1:8080/v1",
      "MaxInputTokens": 28000,
      "MaxOutputTokens": 4096,
      "ReasoningEffort": "off",
      "Streaming": false,
      "RequestTimeoutSeconds": 600
    }
  ]
}
