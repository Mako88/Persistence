Implement context/budget awareness in sensor block (Based on real data provided through the model client)
Add toggle_summary_display command (take a list of fragments)
Add summarize_fragments command (this should probably be two commands - one to summarize a given list of fragments
into a single summary fragment, another to just add summaries to existing fragments)
Implement proposals
Figure out how to handle loading working contexts when they're bigger than the available context
Create MCP server hub, with a "catalog" MCP server exposed to start
Switch Weight for Relevance
Allow browsing, filtering, swapping working contexts